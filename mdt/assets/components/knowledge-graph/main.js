const colors={layer:'#4f46e5',asset:'#059669',action:'#dc2626',default:'#6b7280'};fetch('./data.json').then(r=>r.json()).then(drawGraph);function drawGraph(data){const svg=document.getElementById('graph'),panel=document.getElementById('panel'),pos={},cx=450,cy=260,radius=185;data.nodes.forEach((n,i)=>{const a=Math.PI*2*i/data.nodes.length-Math.PI/2;pos[n.id]={x:cx+radius*Math.cos(a),y:cy+radius*Math.sin(a)}});data.edges.forEach(e=>{const s=pos[e.source],t=pos[e.target];const line=document.createElementNS('http://www.w3.org/2000/svg','line');line.setAttribute('x1',s.x);line.setAttribute('y1',s.y);line.setAttribute('x2',t.x);line.setAttribute('y2',t.y);line.setAttribute('class','edge');svg.appendChild(line);const label=document.createElementNS('http://www.w3.org/2000/svg','text');label.setAttribute('x',(s.x+t.x)/2);label.setAttribute('y',(s.y+t.y)/2);label.setAttribute('class','edge-label');label.textContent=e.relation;svg.appendChild(label)});data.nodes.forEach(n=>{const p=pos[n.id],g=document.createElementNS('http://www.w3.org/2000/svg','g');g.setAttribute('class','node');g.setAttribute('transform',`translate(${p.x}, ${p.y})`);const c=document.createElementNS('http://www.w3.org/2000/svg','circle');c.setAttribute('r',34);c.setAttribute('fill',colors[n.type]||colors.default);g.appendChild(c);const text=document.createElementNS('http://www.w3.org/2000/svg','text');text.setAttribute('text-anchor','middle');text.setAttribute('dy','56');text.textContent=n.label;g.appendChild(text);g.addEventListener('click',()=>{const rel=data.edges.filter(e=>e.source===n.id||e.target===n.id);panel.innerHTML=`<h2>${n.label}</h2><p><span class="badge">${n.type}</span></p><p><strong>ID:</strong> ${n.id}</p><h3>关系</h3><ul>${rel.map(e=>`<li>${e.source} → ${e.target} <span class="badge">${e.relation}</span></li>`).join('')||'<li>暂无</li>'}</ul>`});svg.appendChild(g)})}
